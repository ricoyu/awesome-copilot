# copilot-search → copilot-search-8.x ElasticUtils API 迁移对照

## 整体迁移状态

大部分 API 已成功迁移，核心架构保持一致（顶层门面方法 + `Query`/`Aggs`/`Admin`/`Mappings`/`Settings`/`Cluster` 嵌套内部类）。以下聚焦于**尚未迁移**或**签名不兼容**的 API。

---

## 一、完全缺失的方法（8.x 不具备对应重载）

| # | 原模块方法签名 | 缺失原因 |
|---|-------------|---------|
| 1 | `index(String index, String doc)` | 8.x 无 `String` doc 重载，只有 `Object` 版本 |
| 2 | `index(String index, String doc, String id)` | 同上 |
| 3 | `index(String index, String doc, int id)` | 同上 |
| 4 | `create(String index, String doc, String id)` | 8.x 无 `String` doc 重载 |
| 5 | `create(String index, String doc, int id)` | 同上 |
| 6 | `update(String index, String id, String doc)` | 8.x 改为 `update(String, String, Object)` |
| 7 | `update(String index, Integer id, String doc)` | 8.x 改为 `update(String, Integer, Map)` |
| 8 | `upsert(String index, String id, String doc)` | 8.x 改为 `upsert(String, String, Object)` |

> 这 8 个方法都是接受 `String doc`（JSON 字符串）的重载。8.x 统一使用 `Object doc` 参数（内部通过 `toMap()` 自动处理 JSON 字符串/Map/POJO），**功能上可替代但签名不兼容**。

---

## 二、方法名/参数签名变更

| # | 原模块 | 8.x 模块 | 变更说明 |
|---|--------|---------|---------|
| 1 | `Admin.putIndexTemplateByFile(String templateName)` | `Admin.putIndexTemplate(String templateName)` | 方法名改变 |
| 2 | `constantScoreQuery(...)` / `functionScoreQuery(...)` 在顶层 | 移到 `Query` 内部类中 | 调用方式变为 `ElasticUtils.Query.xxx(...)` |
| 3 | `Admin.setReadOnly` 是实例方法 | 改为 static 方法 | 调用方式变化 |
| 4 | `Query.termQuery(String... indices)` | 新增 `Query.termQuery(String field, Object value)` | 8.x 额外的便捷方法 |

---

## 三、实现方式变更（功能等价）

| 方法 | 原模块 | 8.x |
|------|--------|-----|
| `Admin.putIndexTemplate(...)` | HttpUtils 发 HTTP PUT（带重试） | ES 8.x Client API |
| `Mappings.putMapping(String, String)` | HttpUtils 发 HTTP PUT（多 host 重试） | ES 8.x Client API |
| `Settings.putSettings(String, String)` | 同上 | ES 8.x Client API |

---

## 四、已完整迁移（无差异）

- **文档 CRUD**：`get` / `getWithVersion` / `mget` / `delete` / `deleteBy` / `exists` / `bulkIndex` / `bulkUpdate` / `docCount` / `ping`
- **Query**：`matchQuery` / `matchAllQuery` / `termQuery` / `termsQuery` / `bool` / `range` / `matchPhraseQuery` / `matchPhrasePrefixQuery` / `exists` / `prefix` / `geoDistance` / `multiMatch` / `queryString` / `uriQuery` / `idsQuery` / `scrollQuery` / `query` / `byId` / `templateQuery`
- **Aggs**：`terms` / `multiTerms` / `range` / `histogram` / `dateHistogram` / `min` / `max` / `avg` / `sum` / `stats` / `cardinality` / `composite` / `totalHits`
- **Admin**：`createIndex` / `existsIndex` / `deleteIndex` / `listIndexNames` / `listIndices` / `createIndexAlias` / `deleteIndexAlias` / `getIndexTemplate` / `deleteIndexTemplate` / `pipeline` / `reindex` / `createSearchTemplate` / `deleteSearchTemplate` / `setReadOnly` / `forceMerge`
- **Mappings**：`getMapping` / `putMapping`
- **Settings**：`update` / `putSettings`
- **Cluster**：`health` / `settings` / `allSettings` / `createMultiFieldAgg`
- **其他**：`suggest` / `termSuggest` / `phraseSuggest` / `completionSuggest` / `contextSuggest` / `analyze` / `updateByQuery`

---

## 五、建议

如果目标是让 copilot-search 原测试无需改动即可在新模块跑通，需要补充 8 个 `String doc` 重载方法，并将 `Admin.putIndexTemplateByFile` 方法名补回（或添加委托方法）。其余 API 已对齐。
